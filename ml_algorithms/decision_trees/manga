Decision Trees
Это ваше двоичное дерево из алгоритмов и структур данных(каждый корневой узел представляет одну входную переменную(x) и
точку разделения этой переменной(при условии, что переменная является числовой).

